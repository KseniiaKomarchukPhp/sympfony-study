<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link href="/css/bootstrap.min.css" rel="stylesheet">
        <link href="/css/blog.css" rel="stylesheet">
     </head>
    <body>
        <div class="container">
            <header class="blog-header py-3">
            <div class="row flex-nowrap justify-content-between align-items-center">
                <div class="col-4 pt-1">
                    <a class="link-secondary" href="#">Підписатись</a>
                </div>
                <div class="col-4 text-center">
                   <h1> <a class="blog-header-logo text-dark" href="{{ path('index_default') }}">Технічний Комітет з Фристайлу</a></h1>
                </div>
                <div class="col-4 d-flex justify-content-end align-items-center">
                    <a class="link-secondary" href="#" aria-label="Search">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="mx-3" role="img" viewBox="0 0 24 24"><title>Шукати</title><circle cx="10.5" cy="10.5" r="7.5"></circle><path d="M21 21l-5.2-5.2"></path></svg>
                    </a>
                    <a class="btn btn-sm btn-outline-secondary" href="#">Увійти</a>
                </div>
            </div>
        </header>

            <div class="nav-scroller py-1 mb-2">
            <nav class="nav d-flex justify-content-between">
                <a class="p-2 link-secondary" href="{{ path('index_default') }}">Головна</a>
                <a class="p-2 link-secondary" href="{{ path('article_show_header', {article: 19}) }}">Партнерські Організації</a>
                <a class="p-2 link-secondary" href="{{ path('article_show_header', {article: 20}) }}">Календар змагань</a>
                <a class="p-2 link-secondary" href="{{ path('article_show_header', {article: 21}) }}">Рейтинг</a>
                <a class="p-2 link-secondary" href="{{ path('article_show', {article: 22 }) }}">Документи</a>
                <a class="p-2 link-secondary" href="https://www.instagram.com/tkfreestyleufrs/" target="_blank">Instagram</a>
                <a class="p-2 link-secondary" href="https://www.facebook.com/TKFreestyleUFRS" target="_blank">Facebook</a>
                <a class="p-2 link-secondary" href="{{ path('feedback_contact') }}">Контакти</a>
                <a class="p-2 link-secondary" href="{{ path ('article_create') }}">Створити</a>
            </nav>
        </div>
        </div>
        <main class="container">
        {% block body %}{% endblock %}
        </main>
            <footer class="blog-footer">
            <p>Тут має бути щось написано 2021.</p>
            <p>
                <a href="#">Догори</a>
            </p>
        </footer>
    </body>
</html>
