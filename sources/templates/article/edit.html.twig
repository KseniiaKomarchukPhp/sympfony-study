{% extends 'base.html.twig' %}
{% block body %}
    <h1>Edit the Article {{ post.name }}</h1>
    <div class="row">
        <div class="col-6">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}